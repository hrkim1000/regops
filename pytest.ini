[pytest]
asyncio_mode = auto
# importlib mode: four services each have a tests/ directory, so package-name collisions are
# guaranteed under the default prepend mode.
addopts = --import-mode=importlib
markers =
    integration: requires the Docker Compose stack (docker compose --profile app up -d)
testpaths = shared/tests services
